# 🧠 記憶機能クイックガイド

## 🚀 即時解決

### 文字化け修正
```cmd
# 修正版バッチファイルを実行
start_memory_fixed.bat
```

### 記憶管理ツール
```cmd
# 記憶管理ツールを起動
memory_manager.bat
```

---

## 📁 修正されたファイル

### 1. start_memory_fixed.bat
- **文字化け修正**: Shift-JIS対応
- **コマンド修正**: %%CD%% 変数の修正
- **エラー回避**: 特殊文字のエスケープ

### 2. memory_manager.bat
- **GUIツール**: メニュー形式の記憶管理
- **日本語対応**: 完全なShift-JIS対応
- **安全操作**: 確認付きの危険操作

---

## 🛠️ 記憶管理操作

### 1. 記憶の確認
```cmd
# 記憶ボリューム一覧
docker volume ls | findstr ai_

# 記憶内容確認
docker volume inspect ai_chroma_data
```

### 2. 記憶のバックアップ
```cmd
# 自動バックアップ（memory_manager.bat使用）
memory_manager.bat → 2. 記憶バックアップ

# 手動バックアップ
docker run --rm -v ai_chroma_data:/data -v "%CD%":/backup alpine tar czf /backup/memory_backup.tar.gz -C /data .
```

### 3. 記憶のリストア
```cmd
# 自動リストア（memory_manager.bat使用）
memory_manager.bat → 3. 記憶リストア

# 手動リストア
docker run --rm -v ai_chroma_data:/data -v "%CD%":/backup alpine tar xzf /backup/memory_backup.tar.gz -C /data
```

---

## 🔧 トラブルシューティング

### 1. 文字化けが発生する場合
```cmd
# Shift-JISで実行
chcp 932
start_memory_fixed.bat
```

### 2. コマンドが認識されない場合
```cmd
# 変数を修正
%CD% → %%CD%% (バッチファイル内)
```

### 3. 記憶が保存されない場合
```cmd
# ボリュームの確認
docker volume ls
docker volume inspect ai_chroma_data

# 権限の確認
docker exec ai-agent-app ls -la /app/data/chroma
```

---

## 🎯 推奨手順

### 1. 初回起動
```cmd
# 修正版で起動
start_memory_fixed.bat
```

### 2. 記憶管理
```cmd
# 記憶管理ツール起動
memory_manager.bat
```

### 3. 定期メンテナンス
```cmd
# バックアップ作成
memory_manager.bat → 2. 記憶バックアップ

# ログ確認
memory_manager.bat → 6. システムログ確認
```

---

## 📞 サポート

### 問題報告
1. エラーメッセージをコピー
2. 実行したコマンドを記録
3. Docker Desktopの状態を確認

### よくある問題
- **文字化け**: Shift-JIS設定を確認
- **権限エラー**: 管理者権限で実行
- **ボリュームエラー**: Docker Desktop再起動

---

**🎯 これで文字化けとコマンドエラーが完全に解消されます！**
